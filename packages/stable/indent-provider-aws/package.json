{
  "name": "@indent/provider-aws",
  "main": "lib/index",
  "types": "lib/index",
  "version": "0.0.1-canary.2",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "dependencies": {
    "@indent/base-webhook": "^0.0.1-canary.2",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.89"
  },
  "publishConfig": {
    "access": "public"
  }
}
