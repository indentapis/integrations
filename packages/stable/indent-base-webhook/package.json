{
  "name": "@indent/base-webhook",
  "main": "lib/index.js",
  "types": "lib/index",
  "version": "0.0.1-canary.4",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "publishConfig": {
    "access": "public"
  }
}
