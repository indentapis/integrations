{
  "private": true,
  "name": "@indent/bettercloud-webhook",
  "main": "lib/index",
  "types": "lib/index",
  "version": "0.0.1-canary.12",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "test": "BETTERCLOUD_TOKEN=abcdefghijklmn jest --config ../../../jest.config.json bettercloud.test.*",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "dependencies": {
    "@indent/base-webhook": "^0.0.1-canary.12"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0"
}
