{
  "name": "@indent/dummy-webhook",
  "main": "lib/index",
  "types": "lib/index",
  "version": "0.0.1-canary.7",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "dependencies": {
    "@indent/base-webhook": "^0.0.1-canary.7"
  },
  "peerDependencies": {
    "next": "*"
  },
  "devDependencies": {
    "next": "*"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0"
}
