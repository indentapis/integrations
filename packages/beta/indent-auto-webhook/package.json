{
  "private": true,
  "name": "@indent/new-webhook",
  "main": "lib/index",
  "types": "lib/index",
  "version": "0.0.0",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "test": "jest --config ../../../jest.config.json",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build"
  },
  "dependencies": {
    "@indent/base-webhook": "*",
    "@indent/types": "^0.2.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0"
}
